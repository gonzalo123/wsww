## Enclosing socket.io Websocket server inside a HTML5 SharedWorker

install dependencies:

> npm install

start websocket server

> node io/server.js

start a webserver

> php -S 0.0.0.0:8888 -t www
